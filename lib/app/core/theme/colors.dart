import 'package:flutter/material.dart';

/// Brand colors for the app.
class GreenieColors {
  GreenieColors._();

  // Brand colors
  static const Color fairwayGreen = Color(0xFF1B5E20);
  static const Color teeBoxGreen = Color(0xFF2E7D32);

  /// Primary green used as seed for [ColorScheme].
  static const Color greenPrimary = Color(0xFF26BD00);
}
